# 틈(TEUM) 온보딩 페이지 — 디자인 명세서

> 화면을 그대로 재현하는 데 필요한 **모든 수치**를 담았다. 소스코드 없이 이 표만 보고 스타일을 짤 수 있다.
> 짝 문서: [SPEC_00_README.md](SPEC_00_README.md) · [SPEC_01_CONTENT.md](SPEC_01_CONTENT.md) · [SPEC_03_FUNCTIONAL.md](SPEC_03_FUNCTIONAL.md)

**단위 표기:** 별도 언급이 없으면 `px`. `rem` 기준 1rem = 16px.
**패딩 표기:** CSS 순서(위 오른쪽 아래 왼쪽). 값이 2개면 (세로, 가로).

---

## 1. 디자인 방향

**"금융 앱처럼 차분하되, 스크롤할 때마다 손에 잡히는 것이 생기게."**

| 원칙 | 구체적으로 |
| --- | --- |
| 단일 액센트 | 파랑 하나만 강조색으로 쓴다. 브랜드 색을 여러 개 두지 않는다 |
| 유리 카드 | 카드는 반투명 흰색 + 배경 흐림(blur)으로 배경 위에 떠 있게 한다 |
| 헤드라인에만 무게 | 제목은 아주 크고 굵고 자간을 좁게. 본문은 작고 조용하게 |
| 움직임은 신호로만 | 상태가 바뀔 때만 움직인다. 장식용 루프 애니메이션은 배경 3개로 제한 |
| 전부 변수로 | 색은 CSS 변수로만 쓴다. 하드코딩하면 다크 모드에서 그 부분만 안 바뀐다 |

---

## 2. 색

### 2-1. 토큰 (라이트 / 다크)

CSS 변수 11개 + 이징 1개. 다크 모드는 `prefers-color-scheme: dark`에서 **같은 이름을 재정의**한다.

| 변수 | 라이트 | 다크 | 용도 |
| --- | --- | --- | --- |
| `--ink` | `#16171b` | `#edeff5` | 헤드라인, 최고 명도 텍스트 |
| `--muted` | `#626775` | `#98a1b4` | 본문 |
| `--quiet` | `#9398a4` | `#6d7587` | 캡션, 각주, 플레이스홀더 |
| `--line` | `#e4e7ed` | `#272b36` | 보더, 구분선 |
| `--paper` | `#ffffff` | `#171a22` | 카드·폰 화면 바닥 |
| `--canvas` | `#f7f8fc` | `#0d0f15` | 페이지 바닥 |
| `--blue` | `#3182f6` | `#4d94ff` | 액센트, 주 버튼, 선택 상태 |
| `--blue2` | `#1f6fe5` | `#83b6ff` | 액센트 텍스트, 통계 숫자, 눈썹 |
| `--soft` | `#eaf3ff` | `#152742` | 파랑 계열 옅은 배경 |
| `--lilac` | `#eee9ff` | `#201c36` | 보조 그라디언트 |
| `--mint` | `#e7f8f1` | `#12332a` | 성공·완료 표시 |
| `--ease` | `cubic-bezier(.22, 1, .36, 1)` | 동일 | 모든 트랜지션 공통 이징 |

### 2-2. 토큰 밖 고정 색 (의도적 · 근거 있음)

| 값 | 쓰이는 곳 | 근거 |
| --- | --- | --- |
| `#4B4E58` (다크 `#a7adbb`) | 고정 단위 텍스트 `개월` `일` | **Figma SP-SB-Edit-04 지정색** |
| `#98A2B3` (다크 `#5b6273`) | 캘린더 비활성 날짜 | **Figma SP-SB-Edit-03 지정색** |
| `#F0F7FF` (다크 `#16283f`) | 캘린더 "오늘" 배경 원 | **Figma SP-SB-Edit-03 지정색** |
| `#f04438` | 캘린더 일요일 | 요일 관례 |
| `#a6abb6` (다크 `#5b6274`) | H1 2번째 줄 연회색 | 헤드라인 대비 |
| `#1fa979` | 저장 완료 버튼 | 성공 |
| `#15946b` (다크 `#4fd6a5`) | 필드 완료 ✓ 글자 | 성공 |
| `#14966c` (다크 `#4fd6a5`) | 히어로 노트 ✓ 글자 | 성공 |
| `#b0b4bf` (다크 `#5f6678`) | 플레이스홀더 | |
| `#fff4e3` / `#9c6410` (다크 `#33260f` / `#f0b455`) | 주황 칩 `.chip.beta` | **베타 목표치를 실측치와 구분** |
| `#f1f2f6` / `--quiet` (다크 `rgba(255,255,255,.07)`) | 가상 사례 뱃지 | **신뢰도 낮은 콘텐츠 표시** |
| `#20232b` (다크 `#2b3140`) | 토스트 바닥 | |
| `#ff6b5e` | 실패 토스트 `⚠` | |
| `#151821` (다크 `#080a0f`) | `#result` 섹션 바닥 | 원래 어두운 섹션 |
| `#e50914` `#ff4f8b` `#ff3131` | (구버전 잔재) 로고 자리 색 | 실제 로고 이미지로 대체됨 |

> 주황 칩과 뱃지는 **장식이 아니라 콘텐츠 무결성 장치**다. 색은 바꿔도 되지만
> "일반 정보와 확실히 구별된다"는 성질은 유지해야 한다.

---

## 3. 타이포그래피

**서체:** `Noto Sans KR` (Google Fonts) — weight 400 / 500 / 600 / 700 / 800 / 900
**폴백:** `system-ui, sans-serif`

### 3-1. 크기 표

`clamp(최소, 뷰포트비율, 최대)` = 화면 폭에 따라 자동으로 커지는 값.

| 역할 | 크기 | 굵기 | 행간 | 자간 |
| --- | --- | --- | --- | --- |
| 히어로 H1 | `clamp(3.3rem, 7vw, 6.8rem)` | 900 | 0.98 | −0.075em |
| 클로징 H2 | `clamp(2.5rem, 6vw, 5rem)` | 900 | 1.08 | −0.065em |
| 안내 섹션 H2 | `clamp(2.2rem, 5vw, 4.5rem)` | 900 | 1.12 | −0.06em |
| 결과 섹션 H2 | `clamp(2.5rem, 5vw, 4.6rem)` | 900 | 1.12 | −0.06em |
| 일반 섹션 H2 | `clamp(2rem, 4.4vw, 3.6rem)` | 900 | 1.14 | −0.06em |
| 스텝 카드 H3 | `clamp(2rem, 4vw, 3.5rem)` | 900 | 1.14 | −0.06em |
| 통계 큰 숫자 | `clamp(2.3rem, 4vw, 3.1rem)` | 900 | 1 | −0.055em |
| 히어로 본문 | `clamp(1rem, 1.5vw, 1.2rem)` | 400 | 1.8 | — |
| 스텝 본문 | `1.02rem` | 400 | 1.8 | — |
| 인용문 | `1rem` | 400 | 1.85 | — |
| 카드 본문 / 섹션 리드 | `0.93rem` / 기본 | 400 | 1.75 / 1.8 | — |
| 기능 카드 제목 | `1.36rem` | — | — | −0.045em |
| 통계 카드 제목 | `1.14rem` | 800 | — | −0.035em |
| 눈썹 (eyebrow) | `0.82rem` | 800 | — | — |
| 스텝 번호 라벨 | `0.78rem` | 900 | — | +0.1em |
| 기능 번호 라벨 | `0.73rem` | 900 | — | +0.12em |
| 네비 링크 | `0.9rem` | 700 | — | — |
| 폰: 큰 제목 | `1.36rem` | 900 | — | −0.045em |
| 폰: 필드 라벨 | `0.65rem` | 600 | — | — |
| 폰: 필드 값 | `0.83rem` | 700 | — | −0.02em |
| 폰: 다음결제일 힌트 | `0.62rem` | 600 | — | −0.01em |
| 폰: 안내문 | `0.62rem` | 600 | 1.5 | — |
| 폰: 토스트 | `0.75rem` | 400 | — | — |
| 자동완성 이름 | `0.8rem` | 800 | — | −0.03em |
| 자동완성 부제 | `0.66rem` | 600 | — | — |
| 자동완성 가격 | `0.78rem` | 800 | — | — |
| 캘린더 제목 | `0.92rem` | 900 | — | −0.03em |
| 캘린더 요일 | `0.68rem` | 700 | — | — |
| 캘린더 날짜 | `0.78rem` | 700 | — | — |
| 모달 제목 | `1rem` | 900 | — | −0.04em |
| 모달 본문 | `0.74rem` | 400 | 1.6 | — |
| 칩 | `0.73rem` | 800 | — | −0.01em |
| 뱃지 | `0.68rem` | 800 | — | −0.01em |
| 선택 버튼(choice) | `0.82rem` | 800 | — | — |
| 각주(출처) | `0.73rem` | 400 | 1.75 | — |
| 푸터 | `0.78rem` | 400 | — | — |

### 3-2. 규칙

- **헤드라인이 클수록 자간을 더 조인다** (−0.05 ~ −0.075em). 한글 900 weight는 기본 자간이 넓어 보인다
- **모든 한글 문단에 `word-break: keep-all`.** 없으면 단어 중간에서 줄이 끊긴다
- 본문 행간은 1.75 ~ 1.8 고정
- 브랜드 로고 텍스트 `틈 TEUM`은 900 / 자간 −0.04em / `white-space: nowrap`

---

## 4. 형태 · 그림자 · 레이아웃 상수

### 4-1. 모서리 반경

| 값 | 쓰임 |
| --- | --- |
| `999px` | 네비 바, 네비 링크, 토글 트랙, 폰 노치 |
| `50%` | 캘린더 날짜, 눈썹 점, 히어로 ✓ 배지, 캘린더 화살표 버튼 |
| `48 / 38` | 폰 프레임 / 폰 화면 (900px 이하 `39 / 31`) |
| `38` | 히어로 미리보기 카드 |
| `34` | 스텝 카드 |
| `32` | 결과 보드 |
| `27` | 히어로 미리보기 내부 화면 |
| `26` | 콘텐츠 카드, 인용 카드 |
| `24` | 신뢰지표 박스 |
| `22` | 캘린더 팝업, 이탈 모달 (900px 이하 캘린더는 `18`) |
| `20` | 히어로 요약 박스 |
| `16` | 폰 필드, 버튼, 알림 카드, 자동완성 패널, 히어로 알약, 타임라인 행 |
| `15` | 저장 버튼 |
| `14` | 토스트, D-day 배지 |
| `13` | 모달 버튼, 인물 사각형, (900px 이하) 폰 필드 · 자동완성 패널 |
| `12` | 선택 버튼, 아바타, 히어로 로고, 안내문 |
| `11` | 서비스 로고(기본), 자동완성 행 |
| `10` | 칩 |
| `9` | 자동완성 로고 |
| `8` | 뱃지, 타임라인 겹침 로고 |
| `7` | 작은 서비스 로고(폰 필드 안) |

### 4-2. 그림자

| 대상 | 라이트 | 다크 |
| --- | --- | --- |
| 네비 | `0 12px 40px rgba(35,49,78,.08)` | `0 12px 40px rgba(0,0,0,.42)` |
| 주 버튼 | `0 16px 30px rgba(49,130,246,.26)` | 동일 |
| 히어로 알약 | `0 14px 34px rgba(39,54,87,.12)` | `0 14px 34px rgba(0,0,0,.42)` |
| 히어로 미리보기 | `0 42px 90px rgba(43,57,88,.17)` | `0 42px 90px rgba(0,0,0,.5)` |
| 콘텐츠 카드 hover | `0 24px 56px rgba(30,47,85,.09)` | `0 24px 56px rgba(0,0,0,.35)` |
| 스텝 카드 활성 | `0 30px 70px rgba(30,47,85,.07)` | `0 30px 70px rgba(0,0,0,.35)` |
| 폰 | `0 40px 90px rgba(27,39,70,.18)` | `0 40px 90px rgba(0,0,0,.55)` |
| 자동완성 패널 | `0 22px 44px rgba(28,40,70,.18)` | `0 22px 44px rgba(0,0,0,.55)` |
| 캘린더 | `0 26px 60px rgba(20,30,55,.3)` | `0 26px 60px rgba(0,0,0,.6)` |
| 이탈 모달 | `0 26px 60px rgba(20,30,55,.32)` | `0 26px 60px rgba(0,0,0,.6)` |
| 결과 보드 | `0 40px 90px rgba(0,0,0,.33)` | 동일 |
| 토글 노브 | `0 2px 6px rgba(0,0,0,.13)` | 동일 |

> 다크에서는 색이 섞인 그림자가 탁해 보인다. **전부 순수 검정 알파로 교체**한다.

### 4-3. 섹션별 레이아웃 (데스크톱 기준)

| 섹션 | 최대폭 | 패딩 | 그리드 |
| --- | --- | --- | --- |
| 네비 | `min(1120px, 100% - 32px)` | `10 12 10 20` | flex, 양끝 정렬 |
| 히어로 | `1240` | `150 32 80` | 2단 `1.05fr .95fr`, gap `56`, 세로 가운데, `min-height: 100svh` |
| 신뢰 지표 | `1120` | `0 24`, 아래 여백 `120` | 3단 균등, 박스 안쪽 각 칸 패딩 `26 28` |
| 문제 제기 | `1120` | `110 0 20` | 카드 2단, gap `18`, 카드 그리드 위 여백 `52` |
| 기능 소개 | `1120` | `110 0 20` | 동일 |
| 온보딩 안내 | `1120` | `100 24 70` | 가운데 정렬, 본문 최대폭 `650` |
| 체험 영역 | `1180` | `40 24 120` | 2단 `minmax(350px,.9fr) minmax(420px,1.1fr)`, gap `90`, `align-items: start` |
| 결과 | `1120`(내부) | `140 24` | 2단 `1fr 1fr`, gap `80`, 세로 가운데 |
| 사례 | `1120` | `110 0 20` | 카드 2단, gap `18` |
| 클로징 | `1120` | `150 24` | 가운데 정렬, 본문 최대폭 `560` |
| 푸터 | — | `30` | 가운데 정렬, 위쪽 보더 1px |

**섹션 헤더 공통** (`#problem`, `#features`, `#voices`)
- 가운데 정렬, 좌우 패딩 `24`
- 눈썹 아래 여백 `16`
- H2 아래 리드 문단: 위 여백 `20`, 최대폭 `640`, 가운데

**출처 각주**: 최대폭 `1120`, 위 여백 `24`, 좌우 패딩 `24`

---

## 5. 배경 레이어

페이지 전체에 깔리는 고정 레이어 2장. 둘 다 `position: fixed; inset: 0; pointer-events: none`.

| 레이어 | z-index | 내용 |
| --- | --- | --- |
| `.ambient` | `-2` | 방사형 그라디언트 2개 + 바닥색 |
| `.noise` | `-1` | 미세 노이즈 텍스처, `opacity: .18` (다크 `.1`) |

**ambient 그라디언트 (라이트)**
1. `44rem × 44rem`, 중심 = 마우스 위치(기본 82% / 8%), 색 `rgba(49,130,246,.13)` → 64%에서 투명
2. `38rem × 38rem`, 중심 8% / 38%, 색 `rgba(155,119,246,.11)` → 62%에서 투명
3. 바닥색 `#f7f8fc`

**다크**에서는 알파를 올리고(`.16` / `.13`) 바닥색을 `#0d0f15`로.

**노이즈**는 SVG `feTurbulence`(`fractalNoise`, `baseFrequency .8`, `numOctaves 2`)를 데이터 URI로 깐 사각형, 내부 `opacity .08`.

---

## 6. 컴포넌트 명세

### 6-1. 상단 네비게이션

| 속성 | 값 |
| --- | --- |
| 위치 | `fixed`, `top: 18`(620px 이하 `10`), 가로 가운데, `z-index: 50` |
| 폭 | `min(1120px, 100% - 32px)` |
| 형태 | radius `999`, 배경 `rgba(255,255,255,.78)`, blur `18px`, 보더 `1px rgba(216,218,224,.85)` |
| 패딩 | `10 12 10 20` |
| 링크 | `min-height 44`, 패딩 `0 18`(620px 이하 `0 16`), radius `999`, `.9rem/700`, 색 `--muted` |
| 링크 hover | 배경 `#f0f2f6` (다크 `rgba(255,255,255,.07)`) |
| CTA 버튼 | 배경 `--ink`, 글자 흰색 → **다크에서는 글자를 `#0d0f15`로 반전** |
| 링크 사이 | gap `4` |

**브랜드 마크** (텍스트 왼쪽 도형) — `24 × 24` 영역 안에 사각형 2개를 겹친다.
- 뒤 도형: `12 × 20`, 왼쪽 `3`, 색 `--blue`
- 앞 도형: `10 × 17`, 오른쪽 `1`, 색 `#80b5ff`, `mix-blend-mode: multiply`
- 둘 다 radius `7px 12px 7px 12px`, `rotate(-28deg)` → 기울어진 "틈" 모양

### 6-2. 버튼

| 종류 | 값 |
| --- | --- |
| 공통 | `min-height 54`, radius `16`, 패딩 `0 24`, weight 800, gap `9`, 전이 `.2s` |
| 주 버튼 | 배경 `--blue`, 글자 흰색, 그림자 있음 / hover 시 `translateY(-2px)` + 배경 `--blue2` |
| 보조 버튼 | 배경 `rgba(255,255,255,.7)`(다크 `rgba(255,255,255,.04)`), 보더 `1px --line`, 글자 `--muted` |
| 버튼 묶음 | flex, gap `12`, 위 여백 `34`, 줄바꿈 허용 |

**포커스 링 (공통)**: `outline: 3px solid rgba(49,130,246,.38)`, `outline-offset: 3px`
→ 버튼 / 네비 링크 / 스텝 카드 / 선택 버튼 / 토글에 모두 적용

### 6-3. 눈썹 (eyebrow)

- inline-flex, gap `9`, 아래 여백 `22`(섹션 헤더 안에서는 `16`)
- 색 `--blue2`, `.82rem/800`
- 앞에 점: `7 × 7` 원, 배경 `--blue`, 바깥에 `box-shadow: 0 0 0 6px rgba(49,130,246,.12)` 후광

### 6-4. 히어로 오른쪽 장식

| 요소 | 값 |
| --- | --- |
| 컨테이너 | `min-height 600`(620px 이하 `480`), 내용물 가운데 |
| 링 1 | `490 × 490` 원, 점선 보더 `1px rgba(49,130,246,.22)`, 28초 회전 |
| 링 2 | `350 × 350`, **역방향** 22초 회전 |
| 링(620px 이하) | `390` / `290` |
| 미리보기 카드 | `min(330px, 82vw)`(620px 이하 `270`), 패딩 `18`, radius `38`, 배경 `rgba(255,255,255,.68)`, blur `18`, 보더 `1px #fff`, **`rotate(3deg)`** |
| 카드 hover | `rotate(0) translateY(-6px)`, 전이 `.5s` |
| 내부 화면 | 배경 흰색, 보더 `1px --line`, radius `27`, 패딩 `24 18` |
| 헤더 | flex 양끝, 아래 여백 `24`. 오른쪽 아바타 `34 × 34`, radius `12`, 그라디언트 `135deg, --lilac → #cbdcff` |
| 요약 박스 | 패딩 `20`, radius `20`, 그라디언트 `145deg, #19233a → #2c4777`, 글자 흰색<br>작은 글씨 `.74rem` 색 `#c7d3eb` / 큰 글씨 `1.8rem`, 위 여백 `7` |
| 구독 행 | 3단 `38px 1fr auto`, gap `10`, 패딩 `12 2`, 아래 보더 `1px #f0f1f4`<br>로고 `38 × 38` radius `12` / 부제 `--quiet`, 위 여백 `2` / 금액 `.82rem` |
| 목록 | grid, gap `10`, 위 여백 `16` |
| 떠다니는 알약 | 패딩 `11 14`, radius `16`, 배경 `rgba(255,255,255,.84)`, blur `12`, 보더 `1px #fff`, `.78rem/700`<br>강조 부분(`<b>`) 색 `--blue`. 5초 부유 애니메이션, 각각 `0 / -2.4s / -1s` 지연 |
| 알약 위치 | ① 위 15% 오른쪽 2% ② 아래 18% 왼쪽 0 ③ 위 25% 왼쪽 −2% |

### 6-5. 신뢰 지표

- 박스: 3단 균등 그리드, 보더 `1px --line`, 배경 `rgba(255,255,255,.72)`(다크 `rgba(255,255,255,.045)`), radius `24`, `overflow: hidden`
- 각 칸: 패딩 `26 28`, 오른쪽 보더 `1px --line` (마지막 칸은 없음)
- 큰 글씨 `1.45rem` / 설명 `.84rem` 색 `--muted`, 위 여백 `5`
- **620px 이하**: 1단으로 바뀌고, 오른쪽 보더 대신 **아래** 보더

### 6-6. 콘텐츠 카드 (통계 · 기능 · 인용 공통)

| 속성 | 값 |
| --- | --- |
| 패딩 | `32` (620px 이하 `24 22`) / 인용 카드는 `30 32` |
| radius | `26` |
| 배경 | `rgba(255,255,255,.74)` + blur `12px` (다크 `rgba(255,255,255,.045)`) |
| 보더 | `1px --line` |
| hover | `translateY(-4px)` + 그림자 + 보더 `#cfd9ea`(다크 `#3a4152`), 전이 `.4s` |

**통계 카드 내부**
- 숫자 줄: flex, `align-items: baseline`, gap `9`
  숫자 = 큰 크기 / 900 / 색 `--blue2`, 라벨(`<em>`) = `.9rem/800` 색 `--quiet`
- 제목: 위 여백 `16`, `1.14rem/800`
- 본문: 위 여백 `10`, `.93rem`, 색 `--muted`, 행간 1.75. 본문 안 강조는 `--ink` / 800

**기능 카드 내부** (`display: flex; flex-direction: column`)
- 번호 라벨: `.73rem/900`, 색 `--blue`, 자간 `+.12em`
- 제목: 위 여백 `14`, `1.36rem`, 자간 −0.045em
- 칩 묶음: `margin-top: auto`(카드 높이 맞춤), 위 패딩 `22`, gap `7`

**인용 카드 내부**
- 뱃지 → 인용문(위 여백 `16`, `1rem`, 행간 1.85, 색 `--ink`)
- 인물 줄: 위 여백 `20`, 위 패딩 `18`, 위쪽 보더 `1px --line`, gap `11`
  사각형 `38 × 38` radius `13` 그라디언트 `135deg, --lilac → #cbdcff` / 이름 `.88rem` / 설명 `.75rem` 색 `--quiet`

**칩 / 뱃지**

| 요소 | 패딩 | radius | 배경 | 글자 | 크기 |
| --- | --- | --- | --- | --- | --- |
| 파란 칩 | `7 11` | `10` | `--soft` | `--blue2` | `.73rem/800` |
| 주황 칩 | `7 11` | `10` | `#fff4e3` (다크 `#33260f`) | `#9c6410` (다크 `#f0b455`) | `.73rem/800` |
| 뱃지 | `5 10` | `8` | `#f1f2f6` (다크 `rgba(255,255,255,.07)`) | `--quiet` | `.68rem/800` |

### 6-7. 폰 목업 — 이 페이지에서 가장 중요한 컴포넌트

**기기 프레임**

| 속성 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 크기 | `360 × 690` | `300 × 572` |
| 패딩 | `11` | `11` |
| radius | `48` | `39` |
| 보더 | `1px #d8dce6` (다크 `#2a2f3c`) | 동일 |
| 배경 | `#f6f7fb` (다크 `#14171f`) | 동일 |
| 변형 | `translateY(var(--py)) rotate(var(--pr))`, 전이 `.7s` | 동일 |

- **노치**: `86 × 24`, radius `99`, 배경 `#15171d`, 위 `18`, 가로 가운데, `z-index: 3`

**화면 (`.phone-screen`)**

| 속성 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 높이 | `100%` | 동일 |
| radius | `38` | `31` |
| 패딩 | `55 18 18` | `44 14 14` |
| 배경 | 흰색 (다크 `--paper`) | 동일 |
| 보더 | `1px #e7e9ef` (다크 `--line`) | 동일 |
| 넘침 | **가로 `hidden` / 세로 `auto`** — 폼이 길어 세로로 스크롤된다. 스크롤바는 숨김 | 동일 |

**고정 컨테이너 (`.phone-zone`)**

| 속성 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 위치 | `sticky`, `top: 108` | `sticky`, `top: 70`, `z-index: 10` |
| 높이 | `calc(100svh - 128px)`, 최소 `650` | `auto` |
| 기타 | 내용 가운데 | 패딩 `14 0`, 배경 `linear-gradient(--canvas 78%, transparent)` — 뒤 텍스트가 비치지 않게 |

**후광 (`.halo`)** — 폰 뒤 원형 빛
`520 × 520` 원, `radial-gradient(circle, --soft, rgba(234,243,255,.15) 58%, transparent 72%)`,
`transform: scale(var(--halo))`, 전이 `.8s`

**폰 상단 타이틀**
flex, gap `12`, 아래 여백 `25`. 아이콘 `19 × 19`, 제목 `.94rem`

**폰 큰 제목**: `1.36rem/900`, 자간 −0.045em, 아래 여백 `17` (≤900px `1.05rem` / 여백 `10`)

**폼 스택**: grid, gap `10`(≤900px `7`), 아래 패딩 `26`

### 6-8. 입력 필드

| 상태 | 스타일 |
| --- | --- |
| 기본 | 보더 `1px --line`, radius `16`(≤900px `13`), 패딩 `11 14`(≤900px `7 10`), 배경 흰색(다크 `#1b1f28`), 전이 `.35s` |
| **활성 / 포커스** | 보더 `--blue`, `box-shadow: 0 0 0 4px rgba(49,130,246,.11)`(다크 `rgba(77,148,255,.16)`), 배경 `#fbfdff`(다크 `#1d2432`), **활성일 때만 `translateX(3px)`** |
| **완료** | 우상단에 ✓ — `18 × 18` 원, 위 `9` 오른쪽 `12`, 배경 `--mint`, 글자 `#15946b`, `.68rem/900` |

- 필드는 `position: relative` (✓와 자동완성 패널의 기준점)
- **라벨**: block, `.65rem/600`(≤900px `.55rem`), 색 `--muted`
- **값 줄**: flex, gap `8`, `min-height 25`(≤900px `20`), 위 여백 `3`, `.83rem/700`
- **입력 요소**: 보더·배경·패딩 없는 투명 input. `font: inherit`, `.83rem/700`(≤900px `.7rem`), 자간 −0.02em, `flex: 1`, `appearance: none`
- **플레이스홀더**: 색 `#b0b4bf`, weight 500 (다크 `#5f6678`)
- **오른쪽 아이콘**: `14 × 14`(≤900px `11 × 11`), `margin-left: auto`, `object-fit: contain`, `opacity .5`(다크 `invert(1)` + `.6`)
- **고정 단위** (`개월`/`일`): `.83rem/700`(≤900px `.7rem`), 색 `#4B4E58`, 줄바꿈 금지
- **`원` 접미사**: `.83rem/700`, 색 `--ink`
- **주기 필드의 입력 폭**: `max-width: 3.4em` (숫자 2자리 + 여백)
- **주기 + 금액 한 줄**: 2단 균등 그리드, gap `10`
- **파란 포커스 링은 항상 한 필드에만** (`SPEC_03_FUNCTIONAL.md` §2 참조)

**힌트 줄** (다음 결제일): 위 여백 `2`, `.62rem/600`(≤900px `.55rem`), 색 `--quiet`

### 6-9. 토글 스위치

| 부위 | 값 |
| --- | --- |
| 히트 영역 | `44 × 44`, 배경 투명, 보더 없음 — 터치 타겟 확보용 |
| 트랙 | `42 × 25`, radius `99`, 위치 왼쪽 `1` / 위 `9.5` |
| 트랙 OFF | `#d4d8e1` (다크 `#39404f`) |
| 트랙 ON | `--blue` |
| 노브 | `19 × 19` 원, 흰색(다크 `#e8eaf0`), 그림자 `0 2px 6px rgba(0,0,0,.13)`, 위치 왼쪽 `4` / 위 `12.5` |
| 노브 ON | `translateX(17px)` |
| 전이 | `.35s var(--ease)` |
| 접근성 | `role="switch"` + `aria-checked` |

**무료 체험 줄**: flex, 오른쪽 정렬, gap `8`, 라벨 `.7rem/700` 색 `--muted`

**알림 카드**: flex 양끝, 패딩 `13 14`(≤900px `8 10`), 보더 `1px --line`, radius `16`
- 제목 `.76rem`(굵게) / 설명 `.62rem` 색 `--quiet`, 위 여백 `2`

### 6-10. 자동완성 드롭다운

| 속성 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 위치 | 서비스명 필드 기준 `absolute`, `top: calc(100% + 7px)`, 좌우 `-1`(보더 정렬), `z-index: 20` | `top: calc(100% + 5px)` |
| 컨테이너 | 패딩 `5`, radius `16`, 배경 `--paper`(다크 `#1b1f28`), 보더 `1px --line`, `overflow: hidden` | radius `13` |
| 행 | 3단 `30px 1fr auto`, gap `10`, 패딩 `9 8`, radius `11`, 배경 투명, 왼쪽 정렬 | 3단 `26px 1fr auto`, 패딩 `7 6` |
| 행 사이 선 | `box-shadow: 0 -1px 0 --line` (margin 대신 — 간격이 벌어지지 않게) | 동일 |
| 행 hover | 배경 `--soft`, 구분선 제거 | 동일 |
| 로고 | `30 × 30`, radius `9` | `26 × 26` |
| 이름 | `.8rem/800`, 자간 −0.03em, 색 `--ink` | `.7rem` |
| **검색어 일치 부분** | 같은 줄 안에서 **색만 `--blue`** (굵기·크기 동일) | 동일 |
| 부제 | block, 위 여백 `2`, `.66rem/600`, 색 `--quiet` | `.58rem` |
| 가격 | `.78rem/800`, 줄바꿈 금지 | `.68rem` |
| 결과 없음 | 패딩 `14 10`, `.72rem/600`, 색 `--quiet`, 가운데 정렬 | 동일 |

- 서비스명 필드에 `z-index: 5`를 줘야 패널이 아래 필드들 위로 올라온다
- 접근성: 입력 `role="combobox"` + `aria-expanded`, 패널 `role="listbox"`, 행 `role="option"`

### 6-11. 캘린더 팝업

**오버레이**: 폰 전체 `absolute inset: 0`, `z-index: 40`, 배경 `rgba(16,20,30,.42)` + blur `1px`, radius `38`(폰 화면과 동일), 패딩 `14`, 내용 가운데

| 부위 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 패널 | 폭 100% / `max-width 284`, 패딩 `16 14 12`, radius `22`, 배경 `--paper`(다크 `#1b1f28`) | `max-width 242`, 패딩 `13 11 9`, radius `18` |
| 헤더 | 4단 `28px 1fr 28px 28px`, gap `2`, 아래 여백 `12` | 동일 |
| 제목 | `.92rem/900`, 자간 −0.03em, 가운데 | `.82rem` |
| ‹ › 버튼 | `28 × 28` 원, 배경 투명, 색 `--muted`, `1rem/700`<br>hover 배경 `#f1f3f7`(다크 `#262c39`) + 글자 `--ink` | 동일 |
| × 버튼 | `28 × 28` 원, **배경 항상 `#f1f3f7`**(다크 `#262c39`), `.9rem` | 동일 |
| 요일 줄 | 7단 균등, 아래 패딩 `8`, 아래 여백 `6`, 아래 보더 `1px --line`<br>`.68rem/700` 색 `--muted`, 일=`#f04438`, 토=`--blue` | `.6rem` |
| 날짜 칸 | 7단 균등, `aspect-ratio: 1`, 원형, `.78rem/700`, 색 `--ink` | `.7rem` |
| 일 / 토 | `#f04438` / `--blue` | 동일 |
| **비활성** | 색 `#98a2b3`(다크 `#5b6273`), weight 500, 커서 기본 | 동일 |
| **오늘** | 배경 `#f0f7ff` (다크 `#16283f`) | 동일 |
| **선택일** | 배경 `--blue`, 글자 흰색 | 동일 |
| 날짜 hover | 배경 `--soft` (비활성·선택일 제외) | 동일 |
| 첫 주 빈칸 | 보이지 않는 자리표시자로 채워 요일 정렬 | 동일 |

- 헤더 순서: `‹` · 제목 · `›` · `×`
- 캘린더 열기 버튼(필드 안 아이콘)은 `22 × 22` 투명 버튼

### 6-12. 이탈 확인 모달

**오버레이**: 폰 전체 `absolute inset: 0`, `z-index: 45`, 배경 `rgba(16,20,30,.5)`, 패딩 `20`, radius `38`, 내용 가운데

| 부위 | 데스크톱 | ≤900px |
| --- | --- | --- |
| 패널 | 폭 100%, 패딩 `22 18 18`, radius `22`, 배경 `--paper`(다크 `#1b1f28`), 가운데 정렬 | 동일 |
| 마스코트 | 폭 `74`, 아래 여백 `12`, 가로 가운데 | `60` |
| 제목 | `1rem/900`, 자간 −0.04em | `.88rem` |
| 본문 | 위 여백 `9`, `.74rem`, 행간 1.6, 색 `--muted` | `.66rem` |
| 버튼 줄 | 2단 균등, gap `8`, 위 여백 `16` | 동일 |
| 버튼 | `min-height 42`, radius `13`, `.8rem/800` | 동일 |
| 왼쪽 버튼 | 배경 `#eef0f5`(다크 `#262c39`), 글자 `--ink` | 동일 |
| 오른쪽 버튼 | 배경 `--blue`, 글자 흰색 | 동일 |

### 6-13. 무료 체험 확장 영역

| 상태 | 스타일 |
| --- | --- |
| 접힘 | `max-height: 0`, `opacity: 0`, `overflow: hidden` |
| 펼침 | `max-height: 170px`, `opacity: 1` |
| 전이 | `.45s var(--ease)` |
| 구성 | grid, gap `8` — (체험 기간 필드) + (안내문) |

**안내문**: flex, gap `6`, 패딩 `9 11`(≤900px `7 9`), radius `12`, 배경 `#f2f4f8`(다크 `#20242e`), `.62rem/600`(≤900px `.55rem`), 행간 1.5, 색 `--quiet`

> `max-height`를 쓰므로 **내용이 170px를 넘으면 잘린다.** 필드를 추가하면 값을 올려야 한다.

### 6-14. 저장 버튼 · 토스트

| 상태 | 배경 | 글자 |
| --- | --- | --- |
| 비활성 | `#e8ebf1` (다크 `#242936`) | `#a2a7b1` (다크 `#6d7587`) |
| 활성 | `--blue` | 흰색 |
| 저장 완료 | `#1fa979` | 흰색 |

- 폭 100%, `min-height 48`(≤900px `46`), radius `15`, weight 800

**토스트**

| 속성 | 값 |
| --- | --- |
| 위치 | 폰 기준 `absolute`, 좌우 `29`, 아래 `29` |
| 형태 | 패딩 `13`, radius `14`, 배경 `#20232b`(다크 `#2b3140`), 글자 흰색, `.75rem`, 가운데 정렬 |
| 숨김 | `opacity: 0` + `translateY(18px)` |
| 표시 | `opacity: 1` + 이동 없음, 전이 `.45s` |
| 실패 | `⚠` 글리프 색 `#ff6b5e` / weight 900 |

### 6-15. 스텝 카드 (오른쪽)

| 상태 | 스타일 |
| --- | --- |
| 기본 | `opacity: .4`, `translateY(24px) scale(.98)`, 보더 투명 |
| **활성** | `opacity: 1`, 변형 없음, 배경 `rgba(255,255,255,.74)` + blur `12`(다크 `rgba(255,255,255,.045)`), 보더 `1px --line`, 그림자 |
| 전이 | `.55s var(--ease)` |

| 속성 | 데스크톱 | ≤900px | ≤620px |
| --- | --- | --- | --- |
| 최소 높이 | `78vh` | `66vh` | `60vh` |
| 패딩 | `44` | `34` | `28 22` |
| radius | `34` | 동일 | 동일 |
| H3 크기 | `clamp(2rem,4vw,3.5rem)` | 동일 | `2rem` 고정 |

- 세로 가운데 정렬, 왼쪽 정렬, `outline: 0`, `tabindex="0"`
- 스텝 묶음 위 여백: `10vh` (≤900px `0`)
- 번호 라벨: `.78rem/900`, 색 `--blue`, 자간 `+.1em`
- 제목: 위 여백 `18`
- 본문: 위 여백 `18`, 최대폭 `480`, `1.02rem`, 색 `--muted`, 행간 1.8

**선택 버튼 (choice)**
- 묶음: flex, 줄바꿈 허용, gap `9`, 위 여백 `24`
- 버튼: `min-height 46`, 패딩 `0 15`, radius `12`, 보더 `1px --line`, 배경 흰색(다크 `rgba(255,255,255,.045)`), 색 `--muted`, `.82rem/800`
- hover / 선택됨: 보더·글자 `--blue`(다크 글자 `--blue2`), 배경 `--soft`

### 6-16. 결과 섹션 (어두운 영역)

- 섹션 배경 `#151821`(다크 `#080a0f` + 위아래 보더 `1px --line`), 글자 흰색
- H2 아래 본문 색 `#aeb5c4`, 위 여백 `24`, 행간 1.8

**보드 카드**
- 패딩 `28`(≤620px `18`), radius `32`, 보더 `1px rgba(255,255,255,.09)`
- 배경 `linear-gradient(145deg, #202634, #171b25)` (다크 `#1a1f2b → #12151d`)
- 헤더: flex 양끝. 작은 글씨 색 `#8992a5` / 큰 글씨 `1.8rem`, 위 여백 `5` / 오른쪽 `알림 3` 색 `#6daeff`

**타임라인**
- grid, gap `14`, 위 여백 `30`
- 행: 3단 `50px 1fr auto`(≤620px `46px 1fr`, 값은 2번째 열로 내려감), gap `12`, 패딩 `15`, radius `16`, 배경 `rgba(255,255,255,.055)`
- D-day 배지: `46 × 46`, radius `14`, 배경 `#2c69bd`, `.75rem/900`, 가운데. `position: relative`
- 배지에 겹친 로고: `26 × 26`, radius `8`, 오른쪽 `-7` / 아래 `-7`, 테두리 `0 0 0 2px #171b25`(다크 `#1a1f2b`)
- 제목 `.85rem/700` / 부제 `.85rem` 색 `#8992a5`, 위 여백 `4` / 오른쪽 값 `.82rem`

### 6-17. 클로징 · 푸터

- 마스코트: 폭 `110`, 가로 가운데, 아래 여백 `22`
- 버튼 묶음 가운데 정렬
- CTA 아래 노트: 위 여백 `20`, `.8rem`, 색 `--quiet`, 행간 1.7
- 마지막 한 줄: 최대폭 `640`, 위 여백 `44`, 위 패딩 `30`, 위쪽 보더 `1px --line`, `.95rem`, 색 `--muted`, 행간 1.8
- 푸터: 패딩 `30`, 가운데, 위쪽 보더 `1px --line`, `.78rem`, 색 `--quiet`

### 6-18. 스크롤 진행바

- `fixed`, `top: 0`, 좌우 끝까지, 높이 `3`, `z-index: 60`, `pointer-events: none`
- 안쪽 막대: 폭 100%, `transform: scaleX(진행률)`, `transform-origin: 0 50%`
- 배경 `linear-gradient(90deg, --blue, #9b77f6)`
- 전이 `.12s linear`
- reduced-motion에서는 숨김

### 6-19. 로고 이미지 공통

| 크기 | radius | 쓰이는 곳 |
| --- | --- | --- |
| `38 × 38` | `11` (히어로 목록에서는 `12`) | 히어로 구독 목록 |
| `30 × 30` | `9` | 자동완성 행 |
| `26 × 26` | `8` | 타임라인 D-day 배지 겹침 |
| `22 × 22` | `7` | 폰 서비스명 필드 안 |

공통: `object-fit: cover`, 배경 흰색, `box-shadow: inset 0 0 0 1px rgba(20,25,40,.07)`
→ **흰 배경 로고가 흰 카드에서 묻히지 않게 하는 안쪽 테두리.** 다크에서도 배경은 흰색 유지

---

## 7. 반응형

브레이크포인트는 **900px**와 **620px** 두 개뿐이다.

### 7-1. ≤ 900px — 한 줄 배치로 전환

| 대상 | 변화 |
| --- | --- |
| 히어로 | 1단, 위 패딩 `130`, **글자 가운데 정렬**, 버튼·노트도 가운데 |
| 체험 영역 | 1단, gap `28` |
| 폰 영역 | `sticky top: 70`, 높이 `auto`, 패딩 `14 0`, `z-index: 10`, 뒤에 캔버스색 그라디언트 |
| 폰 | `300 × 572`, radius `39`, 화면 패딩 `44 14 14` / radius `31` |
| 폼 | 큰 제목 `1.05rem` / 여백 `10`, 스택 gap `7`, 필드 패딩 `7 10` / radius `13` |
| 폼 글자 | 라벨 `.55rem`, 값 `.68rem`, 입력 `.7rem`, 단위 `.7rem`, 힌트 `.55rem` |
| 아이콘 | `11 × 11` |
| 알림 카드 | 패딩 `8 10` |
| 저장 버튼 | `min-height 46` |
| 스텝 | 위 여백 `0`, 최소 높이 `66vh`, 패딩 `34` |
| 카드 그리드 | 1단 |
| 섹션 위 패딩 | `110` → `80` |
| 자동완성 | radius `13`, `top: calc(100% + 5px)`, 행 3단 `26px 1fr auto` / 패딩 `7 6` |
| 캘린더 | `max-width 242`, 패딩 `13 11 9`, radius `18` |
| 모달 | 마스코트 `60`, 제목 `.88rem`, 본문 `.66rem` |
| 안내문 | `.55rem`, 패딩 `7 9` |
| 결과 | 1단, 왼쪽 카피 가운데 정렬 |

### 7-2. ≤ 620px — 모바일 마감

| 대상 | 변화 |
| --- | --- |
| 네비 | `top: 10`, **가운데 링크 3개 숨김**, 링크 패딩 `0 16` |
| 히어로 | 패딩 `115 20 50`, gap `0` |
| 오비트 | 최소 높이 `480`, 링 `390` / `290`, 미리보기 `270`, **알약 ③ 숨김** |
| 신뢰 지표 | 1단, 칸 구분선을 오른쪽 → **아래**로 |
| 안내 섹션 | 위 패딩 `70` |
| 체험 영역 | 좌우 패딩 `14` |
| 카드 | 패딩 `24 22`, 그리드 좌우 패딩 `20` / gap `14` / 위 여백 `38` |
| 각주 | 좌우 패딩 `20` |
| 스텝 | 최소 높이 `60vh`, 패딩 `28 22`, H3 `2rem` |
| 결과 | 패딩 `100 20`, 보드 패딩 `18`, 타임라인 2단 (값은 아래 줄로) |
| 클로징 | 패딩 `110 20` |

### 7-3. 가로 스크롤 금지

`html { overflow-x: hidden }` + `body { overflow-x: clip }`
375px 폭에서 `document.documentElement.scrollWidth === 375`여야 한다.

---

## 8. 다크 모드

**`prefers-color-scheme: dark` 자동 전환만 있다. 수동 토글 없음.**

토큰 재정의 외에 **개별로 덮어써야 하는 요소 목록** (하드코딩 색이 있던 곳):

| 요소 | 다크에서 |
| --- | --- |
| `body` / `.ambient` | 캔버스색으로 교체, 그라디언트 알파 상향 |
| `.noise` | `opacity: .1` |
| UI 아이콘 | `filter: invert(1)`, `opacity: .6` |
| 네비 | 배경 `rgba(20,23,31,.74)`, 보더 `rgba(255,255,255,.09)` |
| 네비 CTA | 글자 `#0d0f15`로 반전 |
| 보조 버튼 | 배경 `rgba(255,255,255,.04)`, 보더 `--line` |
| 히어로 알약 | 배경 `rgba(26,30,40,.86)`, 보더 `rgba(255,255,255,.08)` |
| 히어로 미리보기 | 배경 `rgba(26,30,40,.6)`, 보더 `rgba(255,255,255,.08)` |
| 미리보기 내부 화면 | 배경 `--paper`, 보더 `--line` |
| 구독 행 구분선 | `--line` |
| 신뢰 박스 / 카드 / 인용 / 스텝 활성 | 배경 `rgba(255,255,255,.045)`, 보더 `--line` |
| 뱃지 | 배경 `rgba(255,255,255,.07)` |
| 주황 칩 | `#33260f` / `#f0b455` |
| 폰 | 배경 `#14171f`, 보더 `#2a2f3c` |
| 폰 화면 | 배경 `--paper`, 보더 `--line` |
| 필드 | 배경 `#1b1f28` / 활성 `#1d2432`, 포커스 링 `rgba(77,148,255,.16)` |
| 필드 ✓ | 글자 `#4fd6a5` |
| 플레이스홀더 | `#5f6678` |
| 알림 카드 보더 | `--line` |
| 토글 | 트랙 `#39404f`, 노브 `#e8eaf0` |
| 저장 비활성 | 배경 `#242936`, 글자 `#6d7587` |
| 선택 버튼 | 배경 `rgba(255,255,255,.045)` / 선택 시 글자 `--blue2` |
| 자동완성 | 배경 `#1b1f28` |
| 캘린더 | 배경 `#1b1f28`, 화살표 hover `#262c39`, × 배경 `#262c39`, 오늘 `#16283f`, 비활성 `#5b6273` |
| 고정 단위 | `#a7adbb` |
| 안내문 | 배경 `#20242e` |
| 모달 | 배경 `#1b1f28`, 왼쪽 버튼 `#262c39` |
| 토스트 | 배경 `#2b3140` |
| **결과 섹션** | 배경 `#080a0f` + **위아래 보더 `1px --line`** — 원래 어두운 섹션이라 다크에서 구분이 사라지므로 |
| 결과 보드 | `linear-gradient(145deg, #1a1f2b, #12151d)`, 보더 `rgba(255,255,255,.07)` |
| 타임라인 겹침 로고 테두리 | `#1a1f2b` |
| 지표 칸 구분선 / 푸터 보더 | `--line` |
| 로고 이미지 배경 | 흰색 유지 (투명 PNG가 어두운 배경에서 안 보임) |
| ≤900px 폰 영역 그라디언트 | 캔버스색으로 재정의 |

> **다크 모드에서 가장 잘 깨지는 곳:** 결과 섹션(이미 어두움), 보드 카드, 토스트, 폰 내부.
> 이 4곳은 반드시 눈으로 확인할 것.

---

## 9. 모션

### 9-1. 전이 시간

| 대상 | 시간 |
| --- | --- |
| 버튼 hover | `.2s` |
| 진행바 | `.12s linear` |
| 필드 상태 · 토글 | `.35s` |
| 카드 hover | `.4s` |
| 무료체험 펼침 · 토스트 | `.45s` |
| 히어로 카드 hover | `.5s` |
| 스텝 활성화 | `.55s` |
| 폰 변형 | `.7s` |
| 후광 · 등장(reveal) | `.8s` |

**이징은 전부 `cubic-bezier(.22, 1, .36, 1)`** (진행바만 `linear`).

### 9-2. 루프 애니메이션 (배경 전용 · 3개뿐)

| 이름 | 대상 | 값 |
| --- | --- | --- |
| 회전 | 히어로 링 1 | 28초 등속 무한 |
| 회전 | 히어로 링 2 | 22초 등속 무한 **역방향** |
| 부유 | 히어로 알약 3개 | 5초 `ease-in-out` 무한, 중간 지점에서 `translateY(-12px)`. 지연 `0 / -2.4s / -1s` |

### 9-3. 등장 애니메이션

- 대상: 히어로 카피, 히어로 장식, 신뢰 지표, 각 카드, 섹션 헤더, 각주, 결과 카피, 결과 보드, 클로징
- 초기: `opacity: 0` + `translateY(30px)`
- 뷰포트에 **12% 들어오면** `opacity: 1` + 이동 없음
- **1회성** — 한 번 나타나면 다시 감시하지 않는다

### 9-4. 폰 미세 변형 (스텝에 따라)

| 변수 | 계산 | 범위 |
| --- | --- | --- |
| 세로 이동 | `sin(스텝 × 0.9) × 5px` | 약 −5 ~ +5 |
| 회전 | `-1deg + 스텝 × 0.35deg` | −1° ~ +0.75° |
| 후광 크기 | `1 + 스텝 × 0.025` | 1 ~ 1.125 |

스텝이 −1일 때는 0으로 취급한다.

### 9-5. 커서 추적 배경

- `pointermove`로 마우스 좌표를 받아 **보간 계수 0.055**로 부드럽게 따라간다
  (매 프레임 `현재 += (목표 − 현재) × 0.055`)
- 그 좌표를 `.ambient`의 첫 번째 방사형 그라디언트 중심으로 쓴다
- 초기값: 가로 `화면폭 × 0.82`, 세로 `화면높이 × 0.08`
- `requestAnimationFrame` 루프. 리스너는 `{ passive: true }`

### 9-6. `prefers-reduced-motion: reduce`

| 항목 | 처리 |
| --- | --- |
| `html` | `scroll-behavior: auto` |
| 전체 | 모든 `animation`·`transition` 제거 (`!important`) |
| 등장 대상 · 스텝 | 처음부터 완전히 보이게 |
| 스텝 | 최소 높이 해제, 위아래 여백 `24`로 세로 나열 |
| 폰 영역 | `sticky` 해제(`relative`), 회전 0 |
| 진행바 | 숨김 |
| 스크립트 | 스크롤 감시 미설치. **즉시 마지막 스텝(6번) 상태**로 완성된 폼을 보여줌 |

---

## 10. 접근성

| 항목 | 요구 |
| --- | --- |
| 터치 타겟 | 인터랙티브 요소 최소 44 × 46 |
| 포커스 | `:focus-visible`에 `3px solid rgba(49,130,246,.38)`, offset `3` |
| 토글 | `role="switch"` + `aria-checked` |
| 검색 입력 | `role="combobox"` + `aria-expanded` + `aria-controls` |
| 자동완성 패널 / 행 | `role="listbox"` / `role="option"` |
| 캘린더 | `role="dialog"` + `aria-label="날짜 선택"` |
| 이탈 모달 | `role="alertdialog"` + `aria-label="작성 중단 확인"` |
| 폰 영역 | `aria-live="polite"` (값이 바뀌는 것을 알림) |
| 폰 뒤로가기 | `role="button"` + `tabindex="0"` + Enter/Space 지원 |
| 스텝 카드 | `tabindex="0"` |
| 장식 이미지 | `alt=""` |
| 라벨 | 모든 입력에 `<label for>` 연결 |

---

## 11. 디자인 수정 체크리스트

- [ ] 새 색을 CSS 변수 밖에 하드코딩하지 않았는가
- [ ] 다크 모드에서 확인했는가 — 결과 섹션 · 보드 · 토스트 · 폰 내부
- [ ] 한글 문단에 `word-break: keep-all`이 있는가
- [ ] 터치 타겟 44px 이상인가
- [ ] `body { overflow-x: clip }` 유지했는가 (폰 고정)
- [ ] 375px에서 가로 스크롤이 없는가
- [ ] reduced-motion에서 콘텐츠가 전부 보이는가
- [ ] Figma 지정색 3개(`#4B4E58` `#98A2B3` `#F0F7FF`)를 바꾸지 않았는가
- [ ] 주황 칩과 뱃지가 일반 콘텐츠와 시각적으로 구별되는가
